@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@Html.ActionLink(@User.Identity.Name,"Admin","User") </strong>!
    [@Html.ActionLink("Log Off", "LogOff", "User") ]</text>
}
else {
    @: @Html.ActionLink("Logistics", "LogOn", "User") 
}
